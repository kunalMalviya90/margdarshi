# Environment Configuration

# Server Port
PORT=5000

# MongoDB Connection String
# For local MongoDB:
MONGODB_URI=mongodb://localhost:27017/margdarshi
# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/margdarshi

# JWT Secret Key (use a strong random string in production)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# AI Provider Configuration
# Choose ONE of the following AI providers and provide its API key

# Option 1: Groq (RECOMMENDED - Free, Fast, Multilingual)
# Get free API key at: https://console.groq.com/keys
GROQ_API_KEY=your_groq_api_key_here

# Option 2: Google Gemini
# GEMINI_API_KEY=your_gemini_api_key_here

# Option 3: OpenAI
# OPENAI_API_KEY=your_openai_api_key_here

# AI Provider to use (groq, gemini, openai, or gita)
# groq = Groq AI (FREE, fast, excellent for English & Hindi)
# gita = Basic Gita API (FREE, limited responses)
# gemini = Google Gemini Pro (requires API key)
# openai = OpenAI GPT (requires API key)
AI_PROVIDER=groq
